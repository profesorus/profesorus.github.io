#!/bin/bash
#: ---------------------------------------------------------
#: cd "$(dirname "$0")"
cd "${0%/*}"
clear

#: skrypt z komendami startowymi
#: ---------------------------------------------------------
# /bin/bash ./scripts/start_commands.command

gemini

echo
echo " 🔘  press [enter] to exit"
read
